[package]
name = "iceberg-shim"
version = "0.1.0"
description = "Rust cdylib FFI shim for iceberg-rs-mojo: libicebergrsmojo, dlopened at runtime"

[package.build]
backend = { name = "pixi-build-rattler-build", version = "*" }
